/** Function for rendering loading spinner for when things are loading
 */

function Loading() {
    return (
        <div className="spinner-border" style={{ width: "10rem", height: "10rem" }} role="status">
        </div>
      );
}

export default Loading;